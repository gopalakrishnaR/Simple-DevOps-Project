# After updating the code do the following
git status
git add . # or the location of the updated code
git comit -m "reason for the change and commit"
git push origin master #push it to the master branch
